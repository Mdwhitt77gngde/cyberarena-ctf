# Frontend Configuration (.env.example)
# Copia este archivo a .env y ajusta los valores si es necesario

# URL base del backend (FastAPI). Debe coincidir con API_HOST/API_PORT del backend .env
VITE_API_URL=http://localhost:8000
